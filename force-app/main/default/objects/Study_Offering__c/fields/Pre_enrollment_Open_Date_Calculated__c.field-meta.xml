<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Pre_enrollment_Open_Date_Calculated__c</fullName>
    <deprecated>false</deprecated>
    <description>Calculated pre-enrollment open date for status field. Only consider the date in term if the pre-enrollment open date in study offering is empty.</description>
    <formula>IF( ISNULL(  Pre_enrollment_Open_Date__c ) , Academic_Term__r.Pre_enrollment_Open_Date__c , Pre_enrollment_Open_Date__c )</formula>
    <inlineHelpText>Calculated pre-enrollment open date for status field. Only consider the date in term if the pre-enrollment open date in study offering is empty.</inlineHelpText>
    <label>Pre-enrollment Open Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>DateTime</type>
</CustomField>
