<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Start_Date_Calculated__c</fullName>
    <deprecated>false</deprecated>
    <description>Calculated start date for status field. Only consider the dates in term if both start and end date in study offering is empty.</description>
    <formula>IF(ISNULL( Start_Date__c) &amp;&amp; ISNULL(End_Date__c) ,  Academic_Term__r.Start_Date__c , Start_Date__c )</formula>
    <inlineHelpText>Calculated start date for status field. Only consider the dates in term if both start and end date in study offering is empty.</inlineHelpText>
    <label>Start Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
